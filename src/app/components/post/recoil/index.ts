import { atom, selector } from "recoil";

export const TocModalState = atom({
  key: "TocModalState",
  default: false,
});

export const TocResoultionState = atom({
  key: "TocResoultionState",
  default: false,
});
